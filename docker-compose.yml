services:
  mysqldbs:
    image: mysql:8
    container_name: mysql-pro
    environment:
      MYSQL_ROOT_PASSWORD: Venni@07
      MYSQL_DATABASE: tutorfinder
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - springboot-mysql-net

  backend:
    image: boyapativennela/probackend:latest
    container_name: springboot
    ports:
      - "2025:8082"
    depends_on:
      - mysqldbs
    restart: on-failure
    networks:
      - springboot-mysql-net

  frontend:
    image: boyapativennela/profrontend:latest
    container_name: reactpro
    ports:
      - "3002:80"
    depends_on:
      - backend
    restart: on-failure
    networks:
      - springboot-mysql-net

volumes:
  mysql_data:

networks:
  springboot-mysql-net: